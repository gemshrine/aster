{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Main window: core events and app commands",
  "windows": ["main"],
  "permissions": ["core:default", "notification:default"]
}
